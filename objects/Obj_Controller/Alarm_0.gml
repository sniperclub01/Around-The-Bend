/// @description Insert description here
// You can write your code in this editor

room_goto(Room_Action);



/*up = false;
down = false;
left = false;
right = false;

if (ds_grid_get(Obj_Controller.mapGrid, player_X, player_Y-1) == 1) {
	up = true;
}
if (ds_grid_get(Obj_Controller.mapGrid, player_X, player_Y+1) == 1) {
	down = true;
}
if (ds_grid_get(Obj_Controller.mapGrid, player_X+1, player_Y) == 1) {
	right = true;
}
if (ds_grid_get(Obj_Controller.mapGrid, player_X-1, player_Y) == 1) {
	left = true;
}


if (right && !left && !down && !up) {
	startRoom = room_duplicate(Room_Right);
}
if (left && !right && !down && !up) {
	startRoom = room_duplicate(Room_Left);
}
if (up && !left && !right && !down) {
	startRoom = room_duplicate(Room_Up);
}
if (down && !left && !right && !up) {
	startRoom = room_duplicate(Room_Down);
}

room_goto(startRoom);*/
